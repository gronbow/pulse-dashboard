# PR B 实施计划

1. 新增纯函数数据可信度模块，定义日期状态、来源枚举和可访问说明。
2. 扩展快照归一化与发布校验，保留可选 `provenance`，未知字段 fail closed。
3. 将可信度标签接入健康、训练、计划、趋势和紧凑模式。
4. 将洞察标签与活动列表改为安全 DOM 渲染。
5. 更新 Pulse Skill、桥接合同、README 和开发状态，并保持公开插件副本同步。
6. 增加单元、合同和真实 Electron 回归测试，生成合成截图进行视觉验收。
7. 运行完整测试、依赖审计、隐私审计和 Git 差异检查；只提交本地审核，不推送、不合并。
